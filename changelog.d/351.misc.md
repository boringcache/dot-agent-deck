Widen the e2e harness's default wait ceiling (10s to 30s) and two explicit budgets in `prompt/pane-input/007`, so a contention stall on a CI runner no longer reddens the lane-1 tier. Two consecutive `e2e-deterministic` attempts on one tree failed a *different* test each time (20.9s and 18.3s) against a sub-2s healthy path — issue #351's signature. Measured across 2653 shared tests, the median CI/local per-test ratio is 0.96x, so this is contention sensitivity rather than slower hardware. No assertion is weakened: the waits return as soon as their condition holds, so a healthy run pays nothing and a genuine regression still fails.
